[package]
name = "dfmcp-intent"
description = "Semantic intent, immutable plans, bounded obligations, and validation"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
dfmcp-core = { path = "../dfmcp-core" }
dfmcp-world = { path = "../dfmcp-world" }

[lints]
workspace = true
