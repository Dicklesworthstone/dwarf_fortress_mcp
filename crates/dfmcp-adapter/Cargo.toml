[package]
name = "dfmcp-adapter"
description = "Out-of-process game-adapter contract for dwarf_fortress_mcp"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
dfmcp-core = { path = "../dfmcp-core" }
dfmcp-world = { path = "../dfmcp-world" }
dfmcp-intent = { path = "../dfmcp-intent" }

[lints]
workspace = true
