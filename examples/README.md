# Machine-valid protocol examples

These files are complete JSON examples for the phase-zero schemas. Unlike the abbreviated sketches
in `docs/MCP_EXAMPLES.md`, all digests and required fields are syntactically valid. Run
`python3 scripts/validate_repo.py` to validate them.
