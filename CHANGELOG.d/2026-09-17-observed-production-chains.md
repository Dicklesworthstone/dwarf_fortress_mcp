# Coherent observed-stock production chains

- Connect the bounded joint-quota compiler to the existing sealed operations
  views and conservative container/attachment-aware inventory index.
- Require explicit, globally disjoint resource selectors and validate every
  quota/input/output binding; reject wildcard aliases and duplicate outputs.
- Return complete model balances, shortages and dependency steps alongside
  observed stock counts, canonical item examples and disjoint exclusions.
- Bind the full source/anchor/policy and normalized model in a deterministic
  digest without rebuilding generation history or performing game effects.
- Add seven Rust integration tests (not compiled or executed here). An independent
  Python reference passed 24,336 selector-domain intersection comparisons; this
  is mathematical reference evidence, not Rust or native qualification.

See `docs/OBSERVED_PRODUCTION_CHAINS.md`. The API remains read-only, hypothetical
about recipe effects, unadmitted, non-reserving and non-commit-compatible.
