### Native fortress-bound order-condition run foundation

Compose the existing bounded-run clock owner with exact generation/folder/site
and finite manager-order witnesses. Stop on sampled approval, activation or a
remaining-counter threshold; stop separately on disappearance, configuration or
counter/horizon drift. Preserve predicate evidence independently of pause proof,
retain failed safety-stop ownership, and never repeat an attempted unpause.

GCC and Clang each pass 10,422 actual-engine assertions under warning denial and
UBSan, including 256 exhaustive truth schedules. This increment is a callback
engine foundation only, not a native RPC/MCP integration or live qualification.
Existing profiles, production dispatch and compatibility admission are unchanged.
See docs/ORDER_CONDITION_RUN.md for the full scoped contract.
