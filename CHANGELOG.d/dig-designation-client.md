### Executable one-shot designation client and source isolation

Add observe/start/inspect/query/cancel for the separate dig/1.16 developer profile.
Require exact observation and plan confirmation, sync a NEW immutable private
intent file and parent directory before preparation/one-shot dispatch, independently
verify complete native readback, and never replay commit during recovery. Recheck
capsule identity and bytes at effect stages; source loss/absence stays unknown.

Preserve concurrent order-run/1.14 and mining-foundation/1.15 work. The new actual
handler, priority-aware engine and client use independent 1.16 symbols/paths/bytes,
without changing either existing profile. Keep Rust/MCP, the production runner,
compatibility admission, dependencies and existing journals unchanged.

23 executed Python groups cover actual CLI and joined fragmented TCP peers,
POSIX intent custody, separate file/directory sync faults, unchanged-capsule
lost-reply recovery, no redispatch, bounded codecs and full response sizes.
GCC/Clang each reproduce 5,818 assertions plus six rejected mutants against
explicit native SDK/protobuf doubles. No real SDK, live-game, Rust/MCP, power-loss
or full repository qualification claim. See docs/DIG_DESIGNATION_CLIENT.md.
