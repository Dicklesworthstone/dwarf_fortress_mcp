### Expose verified excavation goals in mining recovery Agent Turns

Read optional operator-pinned Python progress journals directly from the existing
query-only mining recovery server. Recompute native samples and goal transitions
with the Rust map codec/evaluator; retain every configured goal beside, not in
place of, pending native-effect identity. Missing/busy/corrupt evidence is unknown,
not absence. Satisfied terrain does not reconcile a designation or permit retry.

Add bounded no-follow read-only snapshots, final complete-byte verification,
session append-prefix/inode pins, current authority floors and complete combined
output reservation. No subprocess, progress write, native sample, new top-level
tool, dependency, native protocol or control-server change. Extend recovery's
operator environment with DFMCP_DIG_GOAL_JOURNALS only.

Nineteen new MCP/archive tests and ten adapter tests remain uncompiled/unexecuted.
Python fixture encoding checks pass (18 positive examples, 12 negative examples,
60 frames, four canonical strings). The same corpus also passes the unchanged
Python monitor replay/evaluator. The new Rust implementation remains unexecuted. See docs/EXCAVATION_MCP_INVENTORY.md.
