### Native workforce assignment and recomputation

Connect workforce/1.17 to six authenticated fixed DFHack RPCs. Capture complete
bounded work details and selected citizen identities/labor masks under suspension;
require enabled automatic professions and selected-only details. Stage replacement
membership and resolve pointers before writes, then recompute changed citizens
through the supported Units API. Preserve verified receipts across response loss,
and retain/block uncertain partial effects without replay or rollback.

GCC and Clang each execute 3,208 actual-handler assertions with explicit SDK/
protobuf API doubles, warning denial and UBSan. Three compiled mutants are
rejected per compiler. No actual SDK/live-game/Rust/MCP or admission claim.
