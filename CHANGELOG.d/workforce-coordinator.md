### Durable Rust workforce assignment and recovery

Connect workforce/1.17 to a source-bound binary journal with synced intent before
prepare, dispatch before assignment and verified receipts before acknowledgement.
Preserve fixed Control/Recover/Offline modes, permanent Unknown, absent-record
uncertainty, cancellation without undo claims and final-evidence reservations.
Add descriptor-pinned private Linux custody with no-follow/nonblocking opens,
exclusive locking and exact-byte checks. No format migration or tail repair.

Twelve Rust groups are registered (23 total), UNCOMPILED/UNEXECUTED because Rust
tooling is unavailable. Six independent Python model groups pass on the 1,803-byte
fixture, corruption/truncation, rehashed illegal paths and retention arithmetic.
No native wire, dependencies, MCP runner, production admission or qualification
claim is changed by this increment.
