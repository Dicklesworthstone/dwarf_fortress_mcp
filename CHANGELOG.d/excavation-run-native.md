# Native excavation-conditioned bounded advancement

Add the isolated excavation-run/1.18 DFHack plugin, six fixed authenticated RPCs,
strict bounded capture/plan/token/receipt encoding and native update-owned stop.
Keep goal evidence separate from historical pause verification; preserve ownership
on capture, setter or reply failures and veto unload while stopping. Hidden terrain
has no payload; independent setter identity checks protect replacement fortresses.

Both GCC and Clang pass 961 actual-handler assertions, five independent native
vectors and four additional compiled mutants, supplementing 7030 engine assertions.
SDK/protobuf doubles are explicit. No real SDK/live-game/Rust/MCP or admission claim.
