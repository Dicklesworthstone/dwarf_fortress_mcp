- Add coherent furniture-construction analysis joining stage, construction/removal
  jobs and optional exact installed-item evidence. Keep job registration, missing
  jobs, suspended work, removal, missing identities and sampled satisfaction
  distinct. Preserve Query authority, enclosing source/anchor/generations and
  bounded complete counts. Rust regression source is added but uncompiled here;
  no native protocol, mutation authority or admission changes.
- Expose construction progress through the shared spatial MCP query handlers,
  with all-target summaries, source-bound pagination, schema discovery and
  explicit generation-pinned watch proposals using the existing engine. Preserve
  removal failure, exact item/container/job links, stale-anchor rejection and
  complete output bounds. Execute independent JSON/predicate reference checks;
  added Rust handler regression source remains uncompiled and unexecuted.
