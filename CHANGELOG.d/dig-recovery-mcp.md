### Expose mining journal recovery through the eleven-tool MCP surface

Add dfmcp-dig-recovery-dev-server over the existing Rust journal and DigSession.
Offline bootstrap/discovery/explanation and paged retained terrain need no native
connection or credential. Operator-enabled wait performs one native effect query
and retains verified evidence before acknowledgement. A separate QueryOnly
source refuses all terrain acquisition and native mutation methods.

All eleven tools return shared Agent Turns with complete bounded inventory,
including pending work beyond page one, exact historical source/receipt references
and explicit unknown/current-state limits. Reserve output and custody reads before
work; stage continuations until post-verification and full rendering succeed.
Own blocking work through the inherited Asupersync region without fallback threads.
Session release preserves every obligation; no game control or journal creation.

The reusable session gains constant-sized pending-work orientation and a single
recovery deadline. Eighteen adapter session and 22 MCP/runtime Rust tests are
registered across these increments, all uncompiled/unexecuted here. The executed
JSON Schema suite passes 24 valid and 87 invalid cases; it is not Rust execution.
No native wire, dependency, production admission or live qualification change.
See docs/DIG_RECOVERY_MCP.md for exact configuration, tools and limitations.
