# Rust excavation-run evidence

- Add sealed capture, region, sampling-spec, plan and receipt types to dfmcp-adapter.
- Decode the existing 1.18 native profile without changing its wire or authority.
- Validate redaction, temporal windows, source identity and terminal immutability.
- Add nine Rust regression tests and six independently constructed byte fixtures.
- Python fixture checking executed; Rust tests are uncompiled and unexecuted.
