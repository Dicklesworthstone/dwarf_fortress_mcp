### Typed workforce adapter and authenticated RPC

Add sealed workforce/1.17 capture, plan and independently reconstructed
membership/labor readback. Bind current named-fortress authority and fixed native
methods, preserve permanent Unknown and absent-record uncertainty, and narrow
absolute socket deadlines without reconnect or replay. Existing native schemas,
dependencies, production runners and admission are unchanged.

Eleven Rust groups are registered but UNCOMPILED/UNEXECUTED: Rust tooling is
unavailable. Seven independent Python reference/static groups pass against the
existing C++ fixtures, with 512 membership cases, 218 byte corruptions and 366
incomplete prefixes. This does not establish Rust/MCP/native/live qualification.
