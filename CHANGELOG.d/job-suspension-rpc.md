# Fixed job-control/1.9 Rust transport

Connect selected-job inspection and sealed suspension plans to the five existing
native RPC methods. Pin protocol, nonce, generation and software identity; reject
method aliases, invalid response shapes and receipts. Bound frame allocation, text
notifications and absolute wall-time across fragmented reads/writes. Concrete TCP
connections are numeric-loopback-only. Every failed wire call fences the stream.

Commit requires exact native prepared evidence. Recovery queries never redispatch;
a missing native record, error envelope or lost reply never proves no effect.
Add nine Rust transport regression groups. They are not compiled or executed in
this environment because Rust/Cargo/rustfmt are unavailable. No real plugin, game,
workspace qualification, production admission or new dependency is claimed.
