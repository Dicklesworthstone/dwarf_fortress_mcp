### Fortress-bound native order-run/1.14 RPC

Expose six fixed authenticated operations and independently scoped native
setters. Capture one selected finite work order with the clock/source witness;
run until a sampled order predicate or safety stop, independently of client
connection lifetime. Preserve separate predicate/pause evidence, stale-plan and
idempotency fences, complete-queue validation, and unload-drain ownership.

Actual plugin translation-unit tests pass 529 assertions and three independent
wire vectors on GCC and Clang with UBSan/warning denial. Three separately built
safety mutants fail on each compiler. SDK/protobuf interfaces are explicit
doubles: no real SDK, live game, Rust/MCP, full qualification or admission claim.
Wire contract, exact limits and operator use: docs/ORDER_CONDITION_RUN_RPC.md.
