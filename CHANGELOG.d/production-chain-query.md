## Agent-facing multi-stage production chains

- Route `production_chain` through the current coherent spatial/1.8 MCP runtime,
  reusing the observed-stock chain compiler and its conservative supply policy.
- Expose complete reserve-aware balances, normalized recipe assumptions, ordered
  dependency steps and every modeled raw-resource deficit through bounded pages.
- Keep model feasibility separate from observed quota satisfaction, native
  readiness and effect authority. Preserve source, session and model cursor fences.
- Add eleven Rust query/actual-handler regression scenarios (not compiled/executed
  here). Executed request-schema tests: 26 accepted, 47 rejected cases.
- No native effects, dependency, archive whitelist or production admission change.
