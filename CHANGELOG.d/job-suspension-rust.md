# Rust evidence for isolated job control/1.9

Add immutable selected-job observations, sealed suspension plans and receipt-verified
effects in dfmcp-adapter. Bind the complete native identity and independently
reconstruct the only permitted readback transition. Keep unknown outcomes distinct
from refusals and observed negative outcomes. Register nine Rust regression groups,
including all-bit fixture corruption and forged-but-rehashed readback.

Independent Python checks reproduced all five hashes in the existing native fixture;
Rust/Cargo/rustfmt are unavailable, so no Rust test execution or qualification is
claimed. No dependency, native wire generation, production admission or MCP tool is
changed. See docs/JOB_SUSPENSION_RUST.md for precise scope and remaining work.
