### Reviewed workforce control and durable recovery

Add the executable workforce/1.17 Python codec/coordinator/CLI with explicit
fortress and citizen selection, reviewed plan confirmation, independent complete
post-witness reconstruction, synced intent/dispatch/receipt ordering, and no
assignment retry after an uncertain dispatch. Retain historical receipts, recover
missing preparation replies by query, retire unreceived native preparations by
cancellation, and preserve permanent Unknown rather than inventing rollback.

Provide private POSIX descriptor custody, strict append-only hash-chain replay,
terminal retention reservations, bounded local discovery, offline inspection,
and complete response reservations before effects. Thirty actual Python test
groups pass, including real files, joined TCP doubles, CLI subprocesses and
independent C++ encoder fixtures. This is not a real SDK/live-game/Rust/MCP,
power-loss, full-repository or production-admission qualification.
