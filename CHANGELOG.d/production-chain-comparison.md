## Same-observation production-alternative comparison

- Add `production_chain_compare` to the coherent spatial/1.8 query path. Evaluate
  2..8 mutually exclusive recipe models against common observed stock and final
  reserves, with one shared planning/frontier work allowance and wall deadline.
- Return complete component-wise resource deficits and a strict Pareto frontier;
  preserve equal/incomparable alternatives without inventing cross-resource cost
  weights or treating batch counts as native time. Reject incomplete comparisons.
- Bind every candidate into source/session/policy continuations, reserve complete
  summaries and rows, and preserve existing watches without additional captures.
- Add ten Rust regression scenarios (not compiled/executed here). Executed Python
  checks: 18 accepted / 40 rejected schema requests, seven schema correspondences,
  729 dominance pairs, 19,683 frontier triples and five mathematical boundaries.
- No native effect, automatic plan selection, archive whitelist, new dependency,
  bridge protocol or production admission change.
