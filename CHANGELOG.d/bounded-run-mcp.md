### Durable bounded runs through the eleven-tool MCP interface

Add an explicitly unadmitted run/1.13 development server backed directly by the
Rust RPC client and effect journal, not a shell/Python subprocess. Wire exact
observation, sealed preparation, confirmed single dispatch, one-sample recovery,
local/native cancellation, bounded record discovery and offline receipt access.
Keep control/recover/offline modes fixed; require operator clock opt-in and
current source-bound authority. Native socket deadlines narrow across foreground
steps. No new top-level tool, dependency, wire generation or production admission.

Use the shared Agent Turn builder with a coordination-root anchor, explicit
non-world scope, historical evidence and active-work discovery. Reserve full
responses and separate journal/native costs before effects. Session close refuses
pending work unless custody is explicitly released for recovery; it never claims
to cancel native ownership or erase evidence. Checkpoint/restore remain refused.

Twelve Rust MCP regression groups are registered but uncompiled/unexecuted (34
across this turn's Rust run integration). Six independent Python model/static
groups pass: eleven-tool wiring, input/reservation bounds, 2,056 pagination cases
and 27 conservative packets at 6,288..14,791 bytes. These do not execute Rust,
MCP, actual filesystem custody, DFHack or a live game. No qualification claimed.
