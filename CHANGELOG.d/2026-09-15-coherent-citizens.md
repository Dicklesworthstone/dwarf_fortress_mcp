## Citizen-inclusive coherent spatial observations

- Added spatial/1.8, a separately versioned development profile that captures the complete strict-citizen roster together with jobs, buildings, inventory and one bounded terrain region during one native RPC suspension.
- Added immutable retained paging with citizen-bound ownership, fixed two-method native RPC registration, dedicated credentials and a 4,096-citizen bound under the existing 16-MiB spatial capture ceiling.
- Added a safe-Rust combined codec and canonical projector with a collision-free citizen entity namespace, strict-citizen membership edges, job `worker_entity` facts and observed citizen-to-job `performs` edges.
- Assigned workers outside the strict-citizen roster remain explicit unknowns rather than fabricated unit entities; an unassigned job records absence.
- Generalized the spatial route/allocation analysis interface so the existing deterministic map and inventory logic runs unchanged against either spatial/1.6 or citizen-inclusive spatial/1.8.
- Registered `dfmcp-live-spatial-citizens-dev-server`, preserving the eleven-tool MCP waist, read-only Observe/Query/Doctor authority, structured queries, graph traversal, baselines, foreground watches, routes and spatial inventory allocation at one combined anchor.
- Added Rust integration scenarios and a reproducible native mock harness. The new 1.8 Rust/native tests were not executed in the editing environment, so no qualification, live-game correctness or admission is claimed.
