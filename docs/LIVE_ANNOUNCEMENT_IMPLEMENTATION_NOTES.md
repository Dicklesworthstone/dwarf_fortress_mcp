# Live announcement implementation notes

The announcement generation is being implemented as a tower of independently testable boundaries:

1. retained-window semantics and canonical identity;
2. exact protobuf request/reply contract;
3. native DFHack extraction with authentication-before-inspection;
4. safe-Rust protobuf validation;
5. bounded complete-window driver;
6. canonical world-event projection and coverage;
7. MCP orientation and querying through the frozen eleven-tool waist;
8. a fresh disposable-fort evidence campaign and exact compatibility admission.

The first two layers are source-complete. They remain prospective and unadmitted until all later layers and live evidence are complete. No existing protocol-1.0 compatibility entry is broadened by these files.