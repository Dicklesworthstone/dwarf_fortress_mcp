# Linked designation and whole-blueprint progress

Implemented `track_dig_blueprint.py`: verify every original dig/1.16 batch receipt,
retain an immutable association to the existing map/1.5 blueprint journal, then
sample the exact complete sparse goal once per foreground request. Fixed deadlines,
simultaneous dry-floor/no-designation predicates, interrupted-read stability reset,
source invalidation, independent receipt revalidation and offline cancellation
compose the existing tested implementations rather than adding a native writer.

Twenty-two new actual Python integration groups pass, alongside the 33 existing
compiler/batch groups. Tests use real files, sync/locking faults, CLI subprocesses
and a joined fragmented TCP double for both fixed protocols. They include 12
sync failure points, 128 retained native steps and the maximum 512-target/1,024-cell
capture. Commands, exact source hashes and measured sizes are retained in
`docs/evidence/linked-blueprint-progress.json`.

No protocol, dependency, production admission, Rust/MCP capability, game-clock
control or native designation path changes. Cross-plugin process/incarnation
identity and mining causality are explicitly unproved. No full workspace,
Rust, SDK, live-game or physical power-loss qualification is claimed.
