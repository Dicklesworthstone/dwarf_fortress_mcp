# Executable exact furnishing batches

The separate `scripts/furniture_batch.py` development workflow executes bounded
32-step exact furniture plans through the existing one-shot furniture/1.19 client.
It persists the complete DAG, source/fortress binding, indexed child-intent custody
and permanent local stop; fresh per-step confirmation and original-key recovery
prevent uncertain or missing history from unlocking later placements. Receipt
export uses the existing construction monitor's exact input shape.

All 35 Python compiler/client/TCP/POSIX/subprocess test functions pass, including
32 actual test-peer placement dispatches, eight unchanged native vectors and 16
publication sync-failure points. Four weakened implementations fail their selected
regressions. Complete maximum-field 32-step output measured 31,548 bytes.
See `docs/FURNITURE_BATCHES.md` and `docs/evidence/furniture-batch.json`.

This is executed development evidence, not real DFHack SDK/plugin, live fortress,
physical power-loss, Rust/MCP or full-workspace qualification. Batches are not
atomic. Placed is historical job registration, not completed construction. No
native protocol, dependency, admission, checkpoint or global-controller guarantee
is introduced. Broader bridge beads remain open.
