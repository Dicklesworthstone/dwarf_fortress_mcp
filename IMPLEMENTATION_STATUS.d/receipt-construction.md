# Receipt-linked construction: executed durable foreground workflow

The separate `scripts/track_construction.py` start/sample/inspect/cancel workflow
connects an exact furniture/1.19 Placed receipt to complete unchanged operations/1.4
observations. It binds the original building footprint/type/stage and exact
item/material identity, checks complete job/attachment conditions and maintains
fixed-deadline sampled stability. Read-only native queries bracket each fresh
capture and verified release with the original receipt on one TCP connection.
Neither a missing job nor a coincident building ID alone establishes completion.

Private append-only monitor journals synchronize the goal/read intent before
native access and complete sample evidence before acknowledgment. Reopen retains
original deadlines, source identities and budgets; interrupted reads reset the
streak. Torn/corrupt history is preserved and refused. Offline inspection and
terminal replay require no credentials or native connection. Cancellation stops
only monitoring. Original placement custody and effect obligations are unchanged.

All 49 actual Python core/TCP/POSIX/CLI test functions pass, including a separate
process completion sample, interrupted-read recovery, synchronization and custody
failures, all 512 item flag words and exact bed/chair/table conditions. Four
weakened core and four weakened publication implementations fail regression
assertions. Exact scope, byte
contracts, usage and input hashes are in `docs/RECEIPT_CONSTRUCTION.md`,
`architecture/construction_monitor_v1.json` and the three construction evidence
reports. This is executed Python/loopback/process evidence, not real native SDK,
live-game, physical power-loss, Rust/MCP or full-workspace qualification. Native
protocols, production admission and dependencies are unchanged. Broader owning
beads df-dfhack-bridge-plane-c-pic.4/.5 remain open.
